import React from 'react';
import preloader from "../../assets/images/Ellipse 4.svg";

export const Preloader = () => {
    return (
        <div>
            <img src={preloader} alt='preloader'/>
        </div>
    );
};
